# Copy to .env and set strong values; compose refuses to start without them.
PII_REDIS_PASSWORD=
PII_MONGO_PASSWORD=
GRAFANA_ADMIN_PASSWORD=
# Optional: HMAC key for payload fingerprints (SHA-256 baseline if empty)
PII_FINGERPRINT_SECRET=
